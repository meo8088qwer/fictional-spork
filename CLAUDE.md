# ROPERANK 작업 규칙

실제 체육관들이 매일 쓰는 서비스. 기능 하나 잘못 건드리면 관장님들이 바로 불편을 겪는다.
2026-09-07에 트리거 컬럼명 오타(`jr.date` → `record_date`)로 전체 체육관의 "학생 삭제"가
8일 넘게 조용히 100% 고장나 있었던 사건 이후 이 규칙을 만듦 — 마이그레이션은 에러 없이
적용됐지만 실제로 실행해보지 않아서 놓쳤다.

## DB 마이그레이션/트리거 작성 시

1. **컬럼명은 기억이 아니라 라이브 스키마에서 확인한다.** `information_schema.columns`로
   실제 컬럼명 조회 후 SQL 작성. 이전 마이그레이션 파일을 보고 추측하지 않는다.
2. **기존 테이블에 트리거/함수를 추가하거나 고칠 때는 그 테이블에 이미 걸려있는
   다른 트리거/함수도 같이 확인한다.** (`select * from pg_trigger where tgrelid = '<table>'::regclass`)
   — 새 기능이 기존 트리거와 순서/조건이 충돌하지 않는지 확인.
3. **적용 후 반드시 실제로 그 경로를 재현 테스트한다.** "마이그레이션이 에러 없이 적용됨"은
   검증이 아니다. 테스트 행 생성 → 변경한 기능 실행 → 결과 확인 → 정리, 까지 라이브로
   한 번은 돌려본다 (impersonation 포함, 필요시).
4. **핵심 CRUD 플로우(학생 추가/삭제, 기록 입력, 종목 추가/삭제, 가입)를 건드리는 변경은
   해당 플로우 전체를 한 번 끝까지 실행해서 확인한다.** 트리거/RLS 하나만 보고 끝내지 않는다.

## 기능 추가/삭제 시 일반

- 기존 기능과의 상호작용을 항상 의심한다: 이 테이블에 걸린 다른 트리거, 이 컬럼을 읽는
  다른 쿼리/RPC, 이 값을 참조하는 프론트 코드가 있는지 grep으로 먼저 확인.
- 배포 후에는 Sentry(`mcp__Sentry__search_issues`, `is:unresolved`)와 Supabase
  advisors(`mcp__Supabase__get_advisors`)를 확인하는 습관을 들인다 — 특히 여러 기능을
  연달아 배포한 뒤에는.
- 사용자가 실사용 중인 서비스이므로, "동작할 것 같다"가 아니라 "동작하는 걸 직접 봤다"만
  완료로 친다.
